export enum EZIndex {
  basic = 1,
  globalLoading = 1000
}

export enum EStorageArea {
  local = "local",
  sync = "sync"
}

export enum EStorageKey {
  appStore = "appStore",
  cacheTime = "cacheTime"
}
